apiVersion: v1
kind: Service
metadata:
  name: colorecho0
  labels:
    app.kubernetes.io/name: colorecho0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: colorecho0
  ports:
    - name: http
      port: 80
      targetPort: http
